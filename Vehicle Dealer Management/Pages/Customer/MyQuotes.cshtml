@page
@model Vehicle_Dealer_Management.Pages.Customer.MyQuotesModel
@{
    ViewData["Title"] = "Báo giá của tôi";
    ViewData["UserRole"] = "CUSTOMER";
    ViewData["UserName"] = HttpContext.Session.GetString("UserName") ?? "Customer";
}

<div class="content-header">
    <h1 class="content-title">Báo giá của tôi</h1>
    <p class="content-subtitle">Xem các báo giá từ đại lý</p>
</div>

@if (Model.Quotes.Any())
{
    @foreach (var quote in Model.Quotes)
    {
        <div class="card mb-3">
            <div class="card-body">
                <div class="flex justify-between items-start mb-3">
                    <div>
                        <h4 style="margin-bottom: 0.5rem;">Báo giá #@quote.Id</h4>
                        <p class="text-muted" style="font-size: 0.875rem; margin-bottom: 0.25rem;">
                            <i class="bi bi-shop"></i> @quote.DealerName
                        </p>
                        <p class="text-muted" style="font-size: 0.875rem; margin: 0;">
                            Ngày tạo: @quote.CreatedAt.ToString("dd/MM/yyyy")
                        </p>
                    </div>
                    @await Html.PartialAsync("_StatusBadge", quote.Status)
                </div>

                <div style="background: var(--surface); padding: 1rem; border: 1px solid var(--border); border-radius: var(--radius-sm); margin-bottom: 1rem;">
                    <div class="flex justify-between items-center mb-2">
                        <span class="text-muted">Số xe:</span>
                        <strong>@quote.VehicleCount xe</strong>
                    </div>
                    <div class="flex justify-between items-center">
                        <span class="text-muted">Tổng giá trị:</span>
                        <strong style="font-size: 1.25rem; color: var(--accent-cyan);">
                            @quote.TotalAmount.ToString("N0") VND
                        </strong>
                    </div>
                </div>

                <div class="flex gap-2">
                    <a href="/Customer/QuoteDetail?id=@quote.Id" class="btn btn-primary btn-sm">
                        <i class="bi bi-eye"></i> Xem chi tiết
                    </a>
                    @if (quote.Status == "SENT")
                    {
                        <button class="btn btn-success btn-sm">
                            <i class="bi bi-check-circle"></i> Chấp nhận
                        </button>
                        <button class="btn btn-secondary btn-sm">
                            <i class="bi bi-x-circle"></i> Từ chối
                        </button>
                    }
                </div>
            </div>
        </div>
    }
}
else
{
    <div class="card">
        <div class="card-body">
            <div class="empty-state">
                <div class="empty-state-icon">📄</div>
                <div class="empty-state-title">Chưa có báo giá</div>
                <div class="empty-state-text">Liên hệ đại lý để nhận báo giá</div>
                <a href="/Customer/Vehicles" class="btn btn-primary mt-3">Xem xe điện</a>
            </div>
        </div>
    </div>
}

