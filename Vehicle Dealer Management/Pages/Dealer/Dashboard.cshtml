@page
@model Vehicle_Dealer_Management.Pages.Dealer.DashboardModel
@{
    ViewData["Title"] = "Dashboard - Nhân viên đại lý";
    ViewData["UserRole"] = "DEALER_STAFF";
    ViewData["UserName"] = Model.UserName;
}

<div class="content-header">
    <h1 class="content-title">Dashboard - @Model.DealerName</h1>
    <p class="content-subtitle">Tổng quan hoạt động bán hàng hôm nay</p>
</div>

<!-- Stats Row -->
<div class="grid grid-cols-4 mb-4">
    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Doanh số hôm nay</div>
            <i class="bi bi-cash-stack" style="font-size: 1.5rem; color: var(--success);"></i>
        </div>
        <div class="stat-value">@Model.TodaySales.ToString("N0")</div>
        <div class="stat-change positive">
            <i class="bi bi-arrow-up"></i>
            <span>+12% so với hôm qua</span>
        </div>
    </div>

    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Đơn hàng</div>
            <i class="bi bi-receipt" style="font-size: 1.5rem; color: var(--accent-cyan);"></i>
        </div>
        <div class="stat-value">@Model.TotalOrders</div>
        <div class="stat-change neutral">
            <span>@Model.PendingOrders đơn chờ xử lý</span>
        </div>
    </div>

    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Báo giá</div>
            <i class="bi bi-file-text" style="font-size: 1.5rem; color: var(--warning);"></i>
        </div>
        <div class="stat-value">@Model.TotalQuotes</div>
        <div class="stat-change neutral">
            <span>@Model.PendingQuotes chờ gửi</span>
        </div>
    </div>

    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Khách hàng</div>
            <i class="bi bi-people" style="font-size: 1.5rem; color: var(--info);"></i>
        </div>
        <div class="stat-value">@Model.TotalCustomers</div>
        <div class="stat-change positive">
            <i class="bi bi-arrow-up"></i>
            <span>+5 khách mới</span>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="grid grid-cols-3 mb-4">
    <a href="/Dealer/Sales/CreateQuote" class="card" style="text-decoration: none; cursor: pointer; border: 2px solid var(--accent-cyan);">
        <div class="card-body text-center">
            <i class="bi bi-plus-circle" style="font-size: 3rem; color: var(--accent-cyan); margin-bottom: 1rem;"></i>
            <h4>Tạo báo giá mới</h4>
            <p class="text-muted">Tạo báo giá cho khách hàng</p>
        </div>
    </a>

    <a href="/Dealer/Customers" class="card" style="text-decoration: none; cursor: pointer;">
        <div class="card-body text-center">
            <i class="bi bi-person-plus" style="font-size: 3rem; color: var(--success); margin-bottom: 1rem;"></i>
            <h4>Thêm khách hàng</h4>
            <p class="text-muted">Tạo hồ sơ khách hàng mới</p>
        </div>
    </a>

    <a href="/Dealer/Vehicles" class="card" style="text-decoration: none; cursor: pointer;">
        <div class="card-body text-center">
            <i class="bi bi-car-front" style="font-size: 3rem; color: var(--warning); margin-bottom: 1rem;"></i>
            <h4>Xem danh mục xe</h4>
            <p class="text-muted">Thông tin xe và tồn kho</p>
        </div>
    </a>
</div>

<!-- Recent Orders -->
<div class="card mb-4">
    <div class="card-header">
        <div class="flex justify-between items-center">
            <h3 class="card-title mb-0">Đơn hàng gần đây</h3>
            <a href="/Dealer/Sales/Orders" class="btn btn-secondary btn-sm">Xem tất cả</a>
        </div>
    </div>
    <div class="card-body">
        @if (Model.RecentOrders.Any())
        {
            <div class="table-container">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Mã đơn</th>
                            <th>Khách hàng</th>
                            <th>Ngày tạo</th>
                            <th>Trạng thái</th>
                            <th>Tổng tiền</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in Model.RecentOrders)
                        {
                            <tr>
                                <td><strong>#@order.Id</strong></td>
                                <td>@order.CustomerName</td>
                                <td>@order.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>@await Html.PartialAsync("_StatusBadge", order.Status)</td>
                                <td><strong>@order.TotalAmount.ToString("N0") VND</strong></td>
                                <td class="text-right">
                                    <a href="/Dealer/Sales/OrderDetail?id=@order.Id" class="btn btn-secondary btn-sm">
                                        Chi tiết
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="empty-state">
                <div class="empty-state-icon">📦</div>
                <div class="empty-state-title">Chưa có đơn hàng</div>
                <div class="empty-state-text">Tạo báo giá và đơn hàng mới</div>
            </div>
        }
    </div>
</div>

<!-- Test Drives & Pending Quotes -->
<div class="grid grid-cols-2">
    <div class="card">
        <div class="card-header">
            <h3 class="card-title mb-0">Lịch lái thử sắp tới</h3>
        </div>
        <div class="card-body">
            @if (Model.TodayTestDrives.Any())
            {
                @foreach (var td in Model.TodayTestDrives)
                {
                    <div class="flex justify-between items-center" style="padding: 0.75rem; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-sm); margin-bottom: 0.5rem;">
                        <div>
                            <div><strong>@td.CustomerName</strong></div>
                            <div class="text-muted" style="font-size: 0.875rem;">@td.VehicleName - @td.Time</div>
                        </div>
                        <div>
                            @await Html.PartialAsync("_StatusBadge", td.Status)
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="empty-state">
                    <div class="empty-state-icon">📅</div>
                    <div class="empty-state-text">Không có lịch lái thử sắp tới</div>
                </div>
            }
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h3 class="card-title mb-0">Báo giá chờ gửi</h3>
        </div>
        <div class="card-body">
            @if (Model.PendingQuotesList.Any())
            {
                @foreach (var quote in Model.PendingQuotesList)
                {
                    <div class="flex justify-between items-center" style="padding: 0.75rem; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-sm); margin-bottom: 0.5rem;">
                        <div>
                            <div><strong>#@quote.Id - @quote.CustomerName</strong></div>
                            <div class="text-muted" style="font-size: 0.875rem;">@quote.CreatedAt.ToString("dd/MM/yyyy")</div>
                            <div style="margin-top: 0.25rem;">
                                @await Html.PartialAsync("_StatusBadge", quote.Status)
                            </div>
                        </div>
                        <div>
                            <a href="/Dealer/Sales/QuoteDetail?id=@quote.Id" class="btn btn-primary btn-sm">Chi tiết</a>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="empty-state">
                    <div class="empty-state-icon">📄</div>
                    <div class="empty-state-text">Không có báo giá chờ gửi</div>
                </div>
            }
        </div>
    </div>
</div>

