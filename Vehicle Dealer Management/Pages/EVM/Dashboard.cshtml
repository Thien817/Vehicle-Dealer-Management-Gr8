@page
@model Vehicle_Dealer_Management.Pages.EVM.DashboardModel
@{
    ViewData["Title"] = "Dashboard - EVM Staff";
    ViewData["UserRole"] = "EVM_STAFF";
    ViewData["UserName"] = Model.UserName;
}

<div class="content-header">
    <h1 class="content-title">Dashboard - EVM Staff</h1>
    <p class="content-subtitle">Quản lý sản phẩm và đại lý</p>
</div>

<!-- Stats Row -->
<div class="grid grid-cols-4 mb-4">
    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Tổng xe</div>
            <i class="bi bi-car-front" style="font-size: 1.5rem; color: var(--accent-cyan);"></i>
        </div>
        <div class="stat-value">@Model.TotalVehicles</div>
        <div class="stat-change neutral">
            <span>@Model.AvailableVehicles xe có sẵn</span>
        </div>
    </div>

    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Tồn kho EVM</div>
            <i class="bi bi-box-seam" style="font-size: 1.5rem; color: var(--success);"></i>
        </div>
        <div class="stat-value">@Model.TotalStock</div>
        <div class="stat-change positive">
            <i class="bi bi-arrow-up"></i>
            <span>+50 xe nhập mới</span>
        </div>
    </div>

    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Đại lý hoạt động</div>
            <i class="bi bi-shop" style="font-size: 1.5rem; color: var(--info);"></i>
        </div>
        <div class="stat-value">@Model.ActiveDealers</div>
        <div class="stat-change neutral">
            <span>/@Model.TotalDealers tổng đại lý</span>
        </div>
    </div>

    <div class="stat-card">
        <div class="flex justify-between items-center">
            <div class="stat-label">Đơn đặt hàng</div>
            <i class="bi bi-truck" style="font-size: 1.5rem; color: var(--warning);"></i>
        </div>
        <div class="stat-value">@Model.PendingDealerOrders</div>
        <div class="stat-change neutral">
            <span>Chờ xử lý</span>
        </div>
    </div>
</div>

<!-- Pending Dealer Orders -->
<div class="card mb-4">
    <div class="card-header">
        <div class="flex justify-between items-center">
            <h3 class="card-title mb-0">Đơn đặt hàng từ đại lý (Chờ duyệt)</h3>
            <a href="/EVM/DealerOrders" class="btn btn-secondary btn-sm">Xem tất cả</a>
        </div>
    </div>
    <div class="card-body">
        @if (Model.DealerOrders.Any())
        {
            <div class="table-container">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Mã đơn</th>
                            <th>Đại lý</th>
                            <th>Ngày đặt</th>
                            <th>Số lượng xe</th>
                            <th>Trạng thái</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var order in Model.DealerOrders)
                        {
                            <tr>
                                <td><strong>#@order.Id</strong></td>
                                <td>@order.DealerName</td>
                                <td>@order.CreatedAt.ToString("dd/MM/yyyy")</td>
                                <td>@order.VehicleCount xe</td>
                                <td>@await Html.PartialAsync("_StatusBadge", order.Status)</td>
                                <td class="text-right">
                                    <a href="/EVM/DealerOrderDetail?id=@order.Id" class="btn btn-success btn-sm">
                                        <i class="bi bi-check-circle"></i> Duyệt
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="empty-state">
                <div class="empty-state-icon">📦</div>
                <div class="empty-state-title">Không có đơn chờ duyệt</div>
                <div class="empty-state-text">Tất cả đơn đặt hàng đã được xử lý</div>
            </div>
        }
    </div>
</div>

<!-- Stock and Dealers -->
<div class="grid grid-cols-2">
    <div class="card">
        <div class="card-header">
            <div class="flex justify-between items-center">
                <h3 class="card-title mb-0">Tồn kho EVM</h3>
                <a href="/EVM/Stocks" class="btn btn-secondary btn-sm">Quản lý</a>
            </div>
        </div>
        <div class="card-body">
            <div class="table-container">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Xe</th>
                            <th>Màu</th>
                            <th>Số lượng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var stock in Model.StockSummary)
                        {
                            <tr>
                                <td><strong>@stock.VehicleName</strong></td>
                                <td>@stock.Color</td>
                                <td>
                                    <span class="@(stock.Qty < 5 ? "text-error" : "")">
                                        @stock.Qty
                                    </span>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <div class="flex justify-between items-center">
                <h3 class="card-title mb-0">Đại lý hoạt động</h3>
                <a href="/EVM/Dealers" class="btn btn-secondary btn-sm">Xem tất cả</a>
            </div>
        </div>
        <div class="card-body">
            @foreach (var dealer in Model.ActiveDealersList)
            {
                <div class="flex justify-between items-center" style="padding: 0.75rem; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-sm); margin-bottom: 0.5rem;">
                    <div>
                        <div><strong>@dealer.Name</strong></div>
                        <div class="text-muted" style="font-size: 0.875rem;">@dealer.Address</div>
                    </div>
                    <div>
                        @await Html.PartialAsync("_StatusBadge", dealer.Status)
                    </div>
                </div>
            }
        </div>
    </div>
</div>

